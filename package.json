{
  "name": "restaurant-app",
  "version": "1.0.0",
  "description": "Full-stack restaurant ordering system",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npx prisma generate",
    "start": "cd server && npx prisma migrate deploy && node src/server.js",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  }
}
